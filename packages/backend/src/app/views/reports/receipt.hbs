<style>
  main {
    display: flex;
    flex-direction: column;
    margin: 60px 0 40px;
  }

  main span {
    text-align: justify;
    font-size: 14px;
  }

  main strong {
    text-transform: uppercase;
  }

  main ul {
    padding: 20px 20px 0;
  }

  main ul li {
    font-size: 14px;
  }

  main ul li+li {
    margin-top: 4px;
  }
</style>

<main>
  <span>
    Declaro que recebi a importância de
    <strong>R$ {{ receiptValue }} ({{ receiptValueExtense }})</strong> referente a(s) nota(s) fiscal(is) abaixo,
    pela transportadora <strong>{{ shippingName }}</strong>
  </span>

  <ul>
    {{#each invoices}}
    {{#with this}}
      <li>{{ number }} - {{ name }}: R$ {{ receiptValue }} ({{ receiptValueExtense }})</li>
    {{/with}}
    {{/each}}
  </ul>
</main>
